---
framework: mocha
preRun:
     command:
        - npm i
preMerge:
  pattern:
    - "test/test.ts"
postMerge:
  pattern:
    - "test/test.ts"
postRun:
     command:
        - node --version
configFile: .mocharc.js
version: 1.1
